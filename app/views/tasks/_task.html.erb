<%= link_to task_path(task) do  %>
  <div class="col s12 m4">
    <div class="card amber z-depth-3">
      <div class="card-content">
        <span class="card-title grey-text text-darken-4"><%= task.title %></span>
        <p class='grey-text text-darken-4 clipped'><%= task.notes %></p>
      </div>
      <div class="card-action">
        <%= link_to task.due || 'Flexible', task_path(task), class: 'blue-text text-darken-3' %>
        <%= link_to 'Learn more', task_path(task), class: 'right blue-text text-darken-3' %>
      </div>
    </div>
  </div>
<% end %>
